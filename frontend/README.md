# VEDAS Pro – Frontend (Vercel Ready)

This is a Vite + React + Tailwind + Recharts frontend wired to your backend at:

```
https://final-year-project-2026-y128.onrender.com
```

## Run Locally

```bash
npm install
npm run dev
# open http://localhost:5173
```

## Deploy to Vercel

```bash
npm install -g vercel
vercel
# accept detected Vite settings
```

No environment variables are required; the backend URL is hard-coded in `src/App.jsx` as `BACKEND_URL`.
